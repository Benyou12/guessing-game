<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="880dp"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="500dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/textView_lobby_title"
            android:tag="textView_lobby_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="16dp"
            android:text="Still waiting for 4 players"
            android:textColor="?attr/textViewColor"
            android:textSize="32sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/lobby_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:layout_marginBottom="3dp"
            android:background="?attr/editTextRounded"
            android:paddingHorizontal="16dp"
            android:paddingVertical="16dp"
            android:textColor="?attr/textViewColor"
            android:textSize="14sp" />
        <include
            android:id="@+id/player_row1"
            layout="@layout/layout_player_row_lobby" />

        <include
            android:id="@+id/player_row2"
            layout="@layout/layout_player_row_lobby" />

        <include
            android:id="@+id/player_row3"
            layout="@layout/layout_player_row_lobby" />

        <include
            android:id="@+id/player_row4"
            layout="@layout/layout_player_row_lobby" />

        <Button
            android:id="@+id/button_start_game"
            android:layout_width="210dp"
            android:layout_height="37dp"
            android:layout_margin="16dp"
            android:layout_marginStart="16dp"
            android:layout_marginLeft="16sp"
            android:layout_marginTop="32dp"
            android:layout_marginEnd="16sp"
            android:layout_marginBottom="0dp"
            android:background="@drawable/rounded_button"
            android:paddingHorizontal="16dp"
            android:tag="button_start_game"
            android:textColor="@color/white"
            android:textSize="14sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>